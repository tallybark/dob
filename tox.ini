# FIXME/2018-06-13: (lb): Resolve differencs between nark and dob's tox.ini's.

[tox]
# FIXME/2018-05-12: (lb): What about py35? (Which CLI supports.) Or py26?
envlist = py27, py34, docs, flake8, isort, manifest, pep257

# FIXME/2018-06-13: (lb): This is in nark's tox.ini:
#  [tox:travis]
#  2.7 = py27
#  3.4 = py34, flake8, pep257, isort, docs

[testenv]
setenv =
    PYTHONPATH = {toxinidir}:{toxinidir}/dob
whitelist_externals = make
passenv =
    SPHINXOPTS_BUILD
    SPHINXOPTS_LINKCHECK
commands =
	pip install -r requirements/dev.pip
	make coverage

[testenv:flake8]
basepython = python3.4
deps =
    flake8==2.5.4
    flake8-debugger==1.4.0
    flake8-print==2.0.2
    pep8-naming==0.3.3
skip_install = True
commands = flake8 setup.py dob/ tests/

[testenv:pep257]
basepython = python3.4
skip_install = True
deps =
    pep257==0.7.0
commands =
    pep257 setup.py dob/ tests/

[testenv:isort]
basepython = python3.4
deps = isort==4.2.5
skip_install = True
commands =
    isort --check-only --recursive --verbose setup.py dob/ tests/

[testenv:manifest]
basepython = python3.4
deps = check-manifest==0.31
skip_install = True
commands =
    check-manifest -v

[testenv:docs]
basepython = python3.4
deps = doc8==0.7.0
commands =
    pip install -r requirements/docs.pip
    make docs BUILDDIR={envtmpdir} SPHINXOPTS={env:SPHINXOPTS_BUILD:''}
    make --directory=docs linkcheck BUILDDIR={envtmpdir} SPHINXOPTS={env:SPHINXOPTS_LINKCHECK:}
    doc8
