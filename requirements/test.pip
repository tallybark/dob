# This file is used for local testing with and without tox,
# as well as for testing on the CI servers.

# Fakers and Factories.
factory-boy == 2.11.1
Faker == 1.0.2
fauxfactory == 3.0.2
pytest-factoryboy == 2.0.2
pytest-faker == 2.0.0

# The rest.
coverage >= 4.5.2
freezegun >= 0.3.11
future == 0.17.1
# EVENTUALLY/2019-02-16: (lb): Use latest: pytest >= 4.2.1
pytest == 3.10.1
pytest-cov == 2.6.1
pytest-mock == 1.10.1

